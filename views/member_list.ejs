<div class="member-list-content">

    <% data.forEach(member=> { %>

        <a href="/profile/<%= member.user_id %>">

            <div class="member">

                <div class="img-wrap">
                    <img class="pfp" src="<%= member.pfp || '/uploads/default_icon.png' %>" alt="<%= member.name %>">
                </div>

                <div class="info">

                    <div class="name">
                        <%= member.name %>
                    </div>

                    <div class="bio">
                        <%= member.bio > 17 ? member.bio.slice(0, 17) + '...' : member.bio || 'Bio' %>
                    </div>

                </div>

            </div>

        </a>

        <% }) %>

</div>
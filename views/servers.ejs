<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Server List</title>
    <link rel="stylesheet" href="/styles/styles.css" />
  </head>
  <body>
    <p>Welcome to the Server List!</p>
    <ul>
      <% servers.forEach(server => { %>
        <li>
          <h3><%= server.name %></h3>
          <p>Private: <%= server.private ? "Yes" : "No" %></p>
          <p>Invite link: <%= server.invite_link %></p>
          <p>Server link: <%= server.server_link %></p>
          <p>Owner username: <%= server.owner_username %></p>
          <p>Owner id: <%= server.owner_id %></p>
        </li>
      <% }) %>
    </ul>
  </body>
</html>
